# training/configs/large.yaml  — 400M tokens, scaling experiment
data_dir: data/processed
max_steps: 100000
batch_size: 8
grad_accum_steps: 8
seq_len: 512
learning_rate: 3e-4
warmup_steps: 2000
eval_every: 1000
save_every: 5000
run_name: hindi-gpt2-400M-tokens
