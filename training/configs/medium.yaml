# training/configs/medium.yaml — 200M tokens, main experiment
data_dir: data/processed
max_steps: 50000
batch_size: 8
grad_accum_steps: 8
seq_len: 512
learning_rate: 3e-4
warmup_steps: 2000
eval_every: 500
save_every: 2000
run_name: hindi-gpt2-200M-tokens
